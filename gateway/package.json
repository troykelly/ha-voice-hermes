{
  "name": "ha-voice-hermes-gateway",
  "private": true,
  "scripts": {
    "build": "worker-build --release",
    "check": "cargo check",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "cargo test"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
